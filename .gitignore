# deps / build
node_modules/
dist/
dist-ssr/
*.local
.ingest/

# env & secrets — NEVER commit
.env
.env.*
!.env.example
secrets/
*.secret
*.secrets
*.creds
credentials*
CREDENTIALS*
service-account*.json
# plaintext creds dropped into the supabase/ working dir (migrations are .sql, never .md)
supabase/*.md
# scratch login/credential drops (e.g. docs/templogins.txt) — never commit
*logins*
templogins*


# contracts / legal — never in any repo (company rule)
contracts/
docs/MOU*
docs/SOW*
docs/INVOICE*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.log
npm-debug.log*

# typescript build info
*.tsbuildinfo

# Jesse's local source drop of the brand logo (canonical web copy is public/mnemosyne-logo.png)
/Mnemosyne logo.png

# Credential files — never commit (Sealed Credential standard; mirror to Mnemosyne instead)
docs/cf-keys.md
*-keys.md
*credentials*
secrets*.md

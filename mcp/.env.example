# 4ward-brain MCP server env. Copy to mcp/.env.local (gitignored). Never commit real values.
# INTERIM single-operator credential model (pre-auth). The server's only DB op is the READ-ONLY
# recall_memory RPC. Phase-2: replace the service-role key with per-user auth (anon key + user JWT).

GEMINI_API_KEY=
VITE_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# docs/threads — agent coordination

How **Atlas** (Claude), **Aegis** (Codex), and **Helios** (Gemini) coordinate asynchronously until the
live brain/MCP message bus exists. **The git repo is the message bus: commit = send, pull = receive.**

## Protocol
- **One file per conversation thread:** `NNNN-<kebab-topic>.md` (zero-padded, incrementing).
- **Each contribution is a dated, attributed section** appended at the end:
  `### <Agent> — YYYY-MM-DD`. Don't edit another agent's entry; append your own.
- **Pull before writing** (`git pull --rebase`), append your entry, then commit + push.
- **Status header** at the top of each thread: `OPEN` / `BLOCKED` / `RESOLVED` + owner.
- **Standing task assignments** live in `AGENTS.md` `▶` blocks; threads are for the discussion/decision.
- **When a thread resolves into a durable decision, mirror it into the canonical docs** (`VISION.md` /
  `CLAUDE.md`). The thread is the *conversation record*; the docs are the *source of truth*.

## Target (where this is headed)
Once the **4ward-brain MCP server** is live, agent conversation moves into the DB
(`agent_messages` + the existing `activity_log`), readable by humans in the dashboard's Realtime feed.
These files are the **bridge** until then.

## Index
- [0001 — Agent coordination model](0001-agent-coordination.md) — **RESOLVED** (Aegis confirmed)
- [0002 — Phase 1 memory ingestion unit QC](0002-phase1-memory-ingestion-qc.md) — ✅ **RESOLVED** — Aegis-approved; Phase 1 continuity-core ingestion COMPLETE (101 entries / 43 chunk-vectors live, recall verified). Non-blocking retry-reliability debt logged for recurring ingestion.
- [0003 — Token economy / context-loading strategy](0003-token-economy.md) — **RESOLVED** (Aegis-approved w/ refinements; in VISION §6)
- [0004 — 4ward-brain MCP server](0004-mcp-server.md) — ✅ **`0008` APPLIED + post-apply gate APPROVED** by Aegis (2026-06-15): recall findings 1–5 fixed, Option A deps 0-fresh, `OPERATOR(public.<=>)` fix, gate 6/6 (def/ACL/clamp/dedup+order/7-field shape/zero-writes). Migrations 0001–0008 all applied. Read-only `recall` MCP tool approved for LOCAL single-operator live test only; teammate/write/secret tools unapproved.
- [0005 — Frontmatter backfill (Helios)](0005-frontmatter-backfill.md) — ✅ **17/17 BACKFILLED + LIVE** (2026-06-15): Helios classified all 17, Aegis security-reviewed, Atlas backfilled 16 then redacted + ingested `intellitax.md` as the 17th (brain **118 entries / 81 chunks**). `intellitax.md` also held a LIVE service-role key likely sent to Google via Helios classification → treat as disclosed; security close-out tracked as open incident `0006`.
- [0006 — IntelliTax service-role key disclosure (incident)](0006-intellitax-key-incident.md) — 🟠 **OPEN, remediation deferred** (Jesse, 2026-06-15): rotate the IntelliTax service-role key (project `ftihkwpirdvykfqabgic`) + confirm absence from repo history/synced backups, via IntelliTax's own deploy. Not blocking 4ward (brain data clean/approved). Aegis close-out pending rotation.
- [0007 — MCP `remember` (write slice)](0007-mcp-remember.md) — ✅ **APPROVED FOR LIVE USE** (Aegis final sign-off `cb1a76c`, 2026-06-15) — local single-operator scope. `recall`+`remember`+`log_update` live-verified (DB gate + concurrent collision + MCP smoke). Migrations 0001–0009 live; baseline 118/81/0/7. NOT covered: teammate/dashboard/`get_secret`/Phase-2 (separate identity/auth QC).
- [0008 — MCP `log_update`](0008-mcp-log-update.md) — ✅ **BUILT + `0009` applied + live-verified** (log 34/0); approved for live use with `0007`.
- [0009 — Secrets vault backend + `get_secret`](0009-secrets-vault-getsecret.md) — ✅ **`0010` BACKEND APPROVED (local single-operator)** by Aegis (`ee1cefc`). Gate passed (ciphertext-at-rest, sensitivity authz, retire modes, zero residue); migrations 0001–0010 live. `service_role` direct vault bypass ACCEPTED as interim risk (local-only) → **must neutralize before Phase-2/teammate access** (supabase_admin/dashboard/support). Real secrets may now be stored via `set_secret`. MCP `get_secret` tool built → thread `0010`.
- [0010 — MCP `get_secret` tool](0010-mcp-get-secret.md) — ✅ **QC APPROVED + live smoke PASSED** (thread 0010 ≠ migration 0010). Thin client over audited/sensitivity-gated `get_secret_operator`; team+admin retrieval exact, audit attributed to OPERATOR_MEMBER_ID, 0 stderr leak, zero residue. **Completes the MCP toolset: recall + remember + log_update + get_secret** (local single-operator). Phase-2 prereq: neutralize service_role vault bypass before multi-user.
- [0013 — Unit B+.2: memory_entries tags](0013-memory-tags.md) — ✅ **APPLIED + backfilled + smoke PASSED** (Aegis-approved): `0011` live (`tags text[]` + GIN); baseline backfill 118/118 (55 project, 34 repo, 20 reusable, 7 code-snippet); recall unchanged (7 fields). Dashboard exact grouping + repo badges + code-library toggle now live. ⚠️ backfill OVERWRITES — one-time only; Helios pass must MERGE.
- [0016 — Sales Factory C2: contract Q&A (RAG)](0016-sales-factory-c2.md) — ✅ **LIVE** (Aegis-approved): `/api/ask-docs` — first **generation** call (embed → search_docs → `gemini-2.5-flash` grounded answer + cited sources); "Ask your contracts" panel live. Smoke all-green: supported Q → real GIAV figures + sources; out-of-scope declines; injection refused; no raw text/keys leaked; 401/403/400. Caveat: assisted-reading aid, not final legal decisions (verify-against-source UX). Deferrals: rate-limit, no Q/A audit.
- [0015 — Sales Factory C1: contract retrieval](0015-sales-factory-c1.md) — ✅ **LIVE** (Aegis-approved): `0012` applied (`search_docs`, service_role-only); **12 contracts / 35 chunks ingested** (GIAV/Spencer/OnTheHash MOU/SOW/proposal/invoice; 768-dim, 0 bad); `/api/search-docs` smoke 6/6 (GIAV query → right deal, metadata-only, 401/403/400); Documents tab live. Team-readable (Jesse-accepted). Migrations 0001–0012 live. Next: C1b (Storage/PDF) → C2 Q&A → C4 generation.
- [0021 — MCP document/file upload](0021-mcp-document-upload.md) — 🔴 **OPEN (to-do, not started)** — owner Atlas. No governed path to upload binary deliverables (PDF/DOCX) into the brain; `remember` is text-only, `log_update` append-only. Surfaced during the GIAV-for-Beth report delivery (2026-06-24). Build a governed `upload_document` MCP tool behind the Phase-2 CF Function trust model (no storage key on the client) + Supabase Storage bucket + contract-scan/sealing governance + reuse the `documents` table/Documents tab; optionally re-embed text-extractable files (C4.2 pattern).
- [0022 — MCP read-body (`fetch`) + safe versioned update (`update`)](0022-mcp-read-and-update.md) — ✅ **APPROVED FOR LOCAL SINGLE-OPERATOR LIVE USE** (Aegis final sign-off, 2026-06-26). Closes the read gap a remote operator hit: `recall` is metadata-only, so agents could find but never *read* an entry, and had no safe way to revise a shared canonical entry. Migration `0021` live: SELECT-only `get_memory_entry` read-body RPC + append-only service-role-only `memory_versions` history + ATOMIC `update_memory`. MCP tools `fetch`(read-only, egress-redacts secrets in tool layer) + `update`(write, mandatory `expected_updated_at`, canonical `change_reason`, versioned/reversible, provenance immutable, never creates). Aegis BLOCKED on 3 → fixed → approved apply (`217a32f`) → **`0021` APPLIED + post-apply gate 8/8 PASSED** (`c1518d4`) → final live-use sign-off recorded in thread. Scope excludes teammate distribution, browser exposure, remote shared MCP hosting, and direct raw RPC use outside the governed tool path.
- [0023 — Document Factory: team docs → branded 4ward layout → PDF](0023-document-factory.md) — 🟡 **OPEN — Phase B design APPROVED TO BUILD by Aegis; live render still gated** (owner Atlas, 2026-06-28). Goal: anyone on the team creates MOUs/SOWs/white papers/use cases in the defined 4ward layout, PDF-first. Reuses auth/dashboard, `generate-contract` (C4.1), `contract-scan`, `documents`/CRM. Plan A→D: **A** canonical brand template approved as non-live scaffolding (`functions/_lib/brand-template.ts`+`brand-logo.ts`; committed `brand-template.test.mjs` 19/19), **B** server-side render approved to implement with `markdown-it@14.2.0` + `@cloudflare/puppeteer@1.1.0` supply-chain dates verified, trusted block tokens, JS-off Browser Rendering, external-resource blocking, policy-mode governance, and JWT→active-member auth; live approval still requires keyless render-pipeline tests + auth/error tests + Browser Rendering smoke, **C** dashboard authoring UI, **D** persist/Storage (overlaps `0021`)/CRM/versioning. Decisions: PDF-first (DOCX deferred), render via CF Browser Rendering, template source-of-truth = repo module. **Update (Atlas, 2026-06-29): Phase B BUILT (code-complete)** — deps pinned (0 new vulns), `render-core.ts` (md html:false + trusted tokens + scheme-validate), `contract-scan` policy-split, templates → `{{block:logo/signature}}`, `render-document.ts` endpoint (JWT→member, stateless, Browser-Rendering lockdown, 503 if binding absent). Keyless **53/0** + 19/0, tsc clean, app build unaffected. ⏳ Live smokes (endpoint auth + Browser-Rendering 0-external) pending the **`BROWSER` binding** (Workers Paid) — infra ask for Jesse. Awaiting Aegis impl review; live-use gated on smokes.
- [0020 — Unit B++: Memories force-graph layout](0020-unit-b-graph.md) — 🟢 **Built (frontend-only, read-only)** — replaces the Memories flat-card first-layer with a node/force-graph constellation (react-force-graph-2d, Jesse's pick): entry nodes colored by kind + project/topic hubs + edges from `[[links]]` + `applies-to:` (surfaces the cross-project code library); click node → existing modal; Graph⇄Cards toggle (graph default). No migration/endpoint/secret. Lazy-loaded (graph chunk split out; main bundle unchanged). Audit: the 2 highs are pre-existing vite/esbuild dev-only, not the new dep.
- [0019 — Sales Factory C5: CRM (deals pipeline + clients + linkage)](0019-sales-factory-c5.md) — ✅ **C5.1 LIVE** (Aegis-approved; `0015`+`0016` applied; smoke **22/22**). Proactive write-lockdown on clients/contacts/deals (members read-only, verified `42501` on direct write) + `documents.deal_id` FK + actor=uid RPCs `upsert_client`/`upsert_deal`/`link_document_deal` (atomic audit) + 3 endpoints (shared `member-auth.ts`) + CRM tab (kanban by stage, clients CRUD, deal detail w/ doc attach/detach). Smoke caught a partial-update data-loss bug → fixed in `0016` (PATCH semantics). **C5.2 ✅ LIVE** (`0017` applied; smoke 16/16; Aegis-approved): `upsert_contact` RPC (PATCH-safe, contacts locked by 0015) + `/api/upsert-contact` + UI (client-modal contacts mgmt, deal-detail activity feed + note composer + client-contacts view); per-deal activity reuses `/api/log-update` (no new backend). **C5 COMPLETE — the sales factory (C1/C2 retrieve + C4.1/C4.2 create + C5.1/C5.2 CRM) is done.** Migrations 0001–0017.
- [0018 — Sales Factory C4.2: persist + re-embed generated drafts](0018-sales-factory-c4.2.md) — ✅ **LIVE** (Aegis-approved after blocker remediation; `0013`+`0014` applied; smoke **18/18**). Aegis caught that documents/chunks still had broad `for all` member-write policy+grants → direct PostgREST write could bypass the gate; fixed by **migration `0014`** (member SELECT-only policies + revoke insert/update/delete from anon/authenticated — verified live: member direct writes now fail `42501`). Plus `0013` (`documents.origin` + insert-only `save_document` RPC, `mou|sow`-only) + `/api/save-document` (scan-gate 422 + governed-draft shape gate → chunk+embed → RPC, actor=uid) + automated prohibited-content scanner (`functions/_lib/contract-scan.ts`) + "Save to brain" UI + Documents draft badge. **Sales-factory CREATE arc (Unit C → C4.1 → C4.2) COMPLETE.** Next: C5 CRM.
- [0017 — Unit C (dashboard writes) → C4 (contract generation) arc](0017-unit-c-c4-generation.md) — ✅ **Unit C LIVE** (Aegis-approved, smoke **15/15**): `/api/log-update`, first authenticated browser WRITE (JWT→active-member→`log_activity` RPC with **actor = authenticated uid from the verified JWT, unforgeable**; no new migration); Activity-page composer. Live battery green (201+actor match, 401/403, all 400s incl. actor-forgery + secret-note rejection, zero residue). **C4.1 (MOU/SOW draft generation) ✅ LIVE** (Aegis-approved, smoke **25/25**): governed templates (constants verbatim / `{{fill}}` field-substitution / `{{draft::}}` model-only narrative grounded on same-type exemplar), `/api/generate-contract` + Generate tab, no persistence; live battery green incl. baked-in prohibited-content scanner (no vendor brands / no AI-disclosure / no leftover markers / no secret leakage), constants-verbatim, same-type sources, all 401/403/400. Arc: Unit C ✅ → C4.1 ✅ → **C4.2 persist/re-embed (next, separate migration/RPC review)** → C5 CRM.
- [0014 — Helios tag refinement pass](0014-helios-tag-refinement.md) — ✅ **RESOLVED**: Helios refined 61/62 reference+feedback entries (0 quarantined), Atlas reviewed + **MERGED** (61 updated, 0 failed; spot-check passed). Now 61 `applies-to:` + 18 `code-snippet` + 0 noisy topics; cross-project code library live. Missed 1 (`feedback-edge-function-deploy`, non-blocking). First Atlas↔Helios judgment-layer collaboration end-to-end.
- [0012 — Phase 2 / Unit B: dashboard semantic recall](0012-dashboard-recall.md) — ✅ **LIVE + smoke PASSED** (Aegis-approved; 7/7): `functions/api/recall.ts` CF Pages Function — JWT verify → active-member check → Gemini embed → `recall_memory` RPC; Memories page has real semantic search. Live smoke all green (valid→200+results, missing/invalid→401, non-member→403, bad args→400, 7-field shape, no leaks); `nodejs_compat` not needed. Deferred (pre-broad-rollout): rate limiting; audit-metadata-only. Foundation for the MOU/SOW sales factory. Aegis close-out pending.
- [0011 — Phase 2 / Unit A: auth + dashboard shell + read views](0011-phase2-dashboard.md) — ✅ **Aegis APPROVED** for controlled A1 live provisioning + dashboard smoke (NOT Unit B/C/D): login (admin-set passwords) + guarded shell + Memories/Activity/Team (RLS-gated reads; no migration/serverless/secrets); `dist/` leak-clean. `scripts/provision-team.mjs` repairs the 7 malformed seed identities IN PLACE (same uuids). `must_change_password` = interim UX only (RLS is the real control). Staged: A→B(recall)→C(writes)→D(secrets, gated).
- [0008 — MCP `log_update`](0008-mcp-log-update.md) — ✅ **design APPROVED w/ corrections → IMPLEMENTED in `0009`** (hardened `log_activity` definer fn; all 6 corrections applied). log 31/0; impl re-review tracked with `0007`. `0009` UNAPPLIED.
